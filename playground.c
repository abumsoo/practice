#include <stdio.h>

/*
int str_len(char *char_ptr)
{
  int length = 0;
  int i = 0;
  while(char_ptr[i] != '\0'){
    length++;
    i++;
  }
  return length;
}

void rev_string(char *original)
{
  int i = 0;
  while(original[i] != '\0'){
    printf("%c", original[i]);
    i++;
  }
}

int main(){
  char og[] = "aohlcrheusrch";
  char *og_ptr = og;
  rev_string(og);
  int length = str_len(og);
  printf("%d\n", length);

  return 0;
}

*/

int main () {
  int sum = 0;
  2+2;
  return 0;
}
